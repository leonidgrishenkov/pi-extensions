{
  "name": "pi-extensions",
  "version": "1.0.0",
  "description": "Personal pi coding agent extensions, themes, and prompts",
  "keywords": [
    "pi-package"
  ],
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .ts",
    "lint:md": "markdownlint 'prompts/**/*.md'",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate:themes": "tsx scripts/validate-themes.ts",
    "audit:prod": "npm audit --omit=dev --audit-level=low",
    "ci": "npm run typecheck && npm run lint && npm run format:check && npm test && npm run validate:themes && npm run lint:md && npm run audit:prod"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.0",
    "turndown": "7.2.4"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.81.1",
    "@earendil-works/pi-coding-agent": "0.81.1",
    "@earendil-works/pi-tui": "0.81.1",
    "@types/turndown": "5.0.5",
    "@typescript-eslint/eslint-plugin": "^8.62.1",
    "@typescript-eslint/parser": "^8.62.1",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "eslint": "^10.6.0",
    "globals": "^17.7.0",
    "markdownlint-cli": "^0.49.0",
    "prettier": "^3.9.3",
    "tsx": "^4.22.4",
    "typebox": "^1.3.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.9"
  },
  "overrides": {
    "fast-uri": "^3.1.4",
    "@hono/node-server": "^2.0.5"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ],
    "themes": [
      "./themes"
    ],
    "prompts": [
      "./prompts"
    ]
  }
}
